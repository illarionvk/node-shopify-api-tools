'use strict'

should = require('chai').should()
expect = require('chai').expect
nock = require('nock')
# api = require('../index.js')

describe('', ->
  it('1 = 1', ->
      x = 1
      x.should.equal(1)
  )
)
